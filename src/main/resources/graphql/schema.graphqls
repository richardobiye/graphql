type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    publicationYear: Int
    publisher: String
    genre: String
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}